# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Jupyter Notebook
.ipynb_checkpoints

# Data (Ignore large files, but keeping folders structure is fine if .gitkeep used, but user asked for folders)
data/raw/*.csv
data/processed/*.csv
# Assuming we want to track the folder structure but not the data itself.

# Models
models/*.pkl
models/*.joblib

# Outputs
outputs/figures/*.png
outputs/figures/*.jpg
outputs/figures/*.svg

# Virtual Environment
faidm/
env/
